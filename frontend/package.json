{
  "name": "dcmview-frontend",
  "version": "0.2.11",
  "private": true,
  "type": "module",
  "engines": {
    "node": "^20.19 || ^22.12 || >=24"
  },
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "check:svelte": "svelte-check --tsconfig ./tsconfig.json",
    "check:ts": "tsc --noEmit",
    "typecheck": "npm run check:svelte && npm run check:ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "generate:types": "python ../scripts/generate_frontend_types.py",
    "check:types": "python ../scripts/generate_frontend_types.py --check",
    "check:contracts": "npm run check:types && python ../scripts/test_generate_frontend_types.py",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^6.2.4",
    "svelte": "^5.56.3",
    "svelte-check": "^4.4.6",
    "typescript": "^5.7.2",
    "vite": "^6.4.1",
    "vitest": "^3.2.4"
  }
}
